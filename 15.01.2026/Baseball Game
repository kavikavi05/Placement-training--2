class Solution:
    def calPoints(self, ops):
        s = []
        for o in ops:
            if o == "+": s.append(s[-1]+s[-2])
            elif o == "D": s.append(2*s[-1])
            elif o == "C": s.pop()
            else: s.append(int(o))
        return sum(s)
