class Solution:
    def findLengthOfLCIS(self, nums):
        ans = cur = 0
        for i in range(len(nums)):
            cur = cur+1 if i>0 and nums[i]>nums[i-1] else 1
            ans = max(ans, cur)
        return ans
