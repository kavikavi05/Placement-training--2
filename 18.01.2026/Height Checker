class Solution {
    public int heightChecker(int[] h) {
        int[] e=h.clone();
        Arrays.sort(e);
        int c=0;
        for(int i=0;i<h.length;i++)
            if(h[i]!=e[i]) c++;
        return c;
    }
}
